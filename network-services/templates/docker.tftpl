version: '3'

services:
  bind9:
    container_name: dns-server
    image: ${image}
    ports:
      - "53:53"
      - "53:53/udp"
    volumes:
      - ./bind:/etc/bind
      - ./bind/cache:/var/cache/bind
      - ./bind/records:/var/lib/bind
    restart: unless-stopped
